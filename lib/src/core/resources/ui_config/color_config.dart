import 'package:flutter/material.dart';

abstract class AppColor {
  AppColor._();

  static const black = Color(0xFF0F0F0F);
  static const white = Colors.white;
  static const blue = Color(0xFF007AFF);
  static const lightGrey = Color(0xFF505153);
}
